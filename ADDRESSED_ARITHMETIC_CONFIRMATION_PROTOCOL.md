# Addressed arithmetic-state cross-model confirmation

Version 2026-07-14-v1. This freezes the positive Qwen v2 discovery onto
Mistral-7B-Instruct-v0.3. Use the identical 12 addition rows, raw neutral digit
prototypes, L2 digit-token intervention, raw embedding baseline, wrong +1
direction, and all Qwen thresholds. No task, scale, or layer selection occurs.

Qwen's earliest matched query-state interchangeability was L26/28. The sole
Mistral workspace checkpoint is frozen to normalized depth L30/32. Confirmation
requires >=90% native CLEAN/NATURAL accuracy, the same neutral consequence gate
(>=80% target accuracy and positive rows, effect ratio [.70,1.30], margin over
wrong direction), and bidirectional matched query-state interchangeability at
L30. Raw embedding equivalence is reported and must not substitute for the
neutral processed-state result.
